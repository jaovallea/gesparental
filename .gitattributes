# ======================================================
# ⚙️ CONFIGURACIÓN GENERAL
# ======================================================

# Forzar finales de línea estilo Unix (LF) para todo archivo de texto
* text=auto eol=lf

# Forzar codificación UTF-8 en todo archivo de texto
* working-tree-encoding=UTF-8

# Detectar correctamente los archivos de texto y su tipo
*.py text diff=python
*.html text diff=html
*.css text diff=css
*.js text diff=javascript
*.json text
*.yml text
*.yaml text
*.toml text
*.ini text
*.cfg text
*.txt text
*.md text
*.rst text

# ======================================================
# 📦 ARCHIVOS BINARIOS
# ======================================================
# Evita que Git intente mostrar diferencias en archivos binarios
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar binary
*.gz binary
*.7z binary
*.exe binary
*.dll binary
*.so binary
*.db binary
*.sqlite binary
*.sqlite3 binary
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary

# ======================================================
# 🚀 DESPLIEGUE Y RENDIMIENTO
# ======================================================

# Ignorar metadatos de sistema
*.log text eol=lf

# Asegurar que los archivos críticos de despliegue sean tratados como texto
render.yaml text eol=lf
requirements.txt text eol=lf

# Optimiza el almacenamiento y evita conversiones innecesarias
*.csv text eol=lf
*.tsv text eol=lf

# ======================================================
# 🧠 PROYECTO APP
# ======================================================

# Forzar tratamiento de plantillas y scripts como texto
app/**/*.html text
app/**/*.js text
app/**/*.py text
app/**/*.css text
app/**/*.jinja text
templates/**/*.html text
static/**/*.css text
static/**/*.js text
